# Task Validator rules

1. Reject tasks that require tables/views outside `tables.md`.
2. Reject tasks that need writes, admin access, or package installation.
3. Accept only modes `analysis`, `chart`, or `both`.
4. If infeasible, explain why in plain language — do not invent a workaround that violates security.
